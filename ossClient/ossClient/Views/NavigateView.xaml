<UserControl x:Class="ossClient.Views.NavigateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             Width="300" Height="600">
    <Grid>
        <ListBox  x:Name="buckets" SelectedIndex="{Binding selectedBuketIndex}" Margin="0,0,0,197">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}"  
                                   Margin='2 0 12 0'   
                                   FontSize='30' />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button x:Name="createBucket" Content="create" HorizontalAlignment="Left" Height="27" Margin="10,431,0,0" VerticalAlignment="Top" Width="106"/>
        <Button x:Name="deleteBucket" Content="delete" HorizontalAlignment="Left" Height="27" Margin="162,431,0,0" VerticalAlignment="Top" Width="106"/>
        <TextBox x:Name="inputBucketName" HorizontalAlignment="Left" Height="27" Margin="36,483,0,0" TextWrapping="Wrap"  VerticalAlignment="Top" Width="201" />
    </Grid>
</UserControl>
