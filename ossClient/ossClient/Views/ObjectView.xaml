<UserControl x:Class="OssClientMetro.Views.ObjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             Height="485.057" Width="489.598">
    <Grid>
        <ListBox  x:Name="objectList" SelectedIndex="{Binding selectedIndex}"  Margin="0,0,0,108" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                   
                        <TextBlock Text="{Binding key}"  
                                       Margin='2 0 12 0'   
                                       FontSize='14' />
             
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="cal:Message.Attach" Value="[Event PreviewMouseDoubleClick] = [OpenFolder()]" />
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
        <Button x:Name="createFolder" Content="Create Folder" HorizontalAlignment="Left" Height="25" Margin="25,418,0,0" VerticalAlignment="Top" Width="123"/>
        <Button x:Name="refresh"  Content="Refresh" HorizontalAlignment="Left" Height="25" Margin="184,418,0,0" VerticalAlignment="Top" Width="104"/>
        <Button x:Name="delete" Content="delete" HorizontalAlignment="Left" Height="25" Margin="327,418,0,0" VerticalAlignment="Top" Width="125"/>
    </Grid>
</UserControl>
